<div class="all">
	<h1>Editing Book</h1>

	<%= form_for(@blog) do |f| %>
		<div class="box">
			<h4>Title</h4>
			<%= f.text_field :title, class: "book_title" %>
		</div>

		<div class="box">
			<h4>Body</h4>
			<%= f.text_area :body, class: "book_body" %>
		</div>

		<div class="box">
			<p><%= f.submit "Update Book" %></p>
		</div>
	<% end %>

	<div class="box">
		<%= link_to "Show", blog_path(@blog), class: "show" %>
		<%= link_to "Back", blogs_path, class: "back" %>
	</div>
</div>